---
dependencies: []
galaxy_info:
  author: k3s-cluster
  description: Install External Secrets Operator and expose the AWS provider connection (ClusterSecretStore) to consuming namespaces
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
  galaxy_tags:
    - secrets
    - aws
    - kubernetes
